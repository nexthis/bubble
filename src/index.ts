// import { makeEventManager } from './eventManager';
// import { makeBubble } from './bubble';

export { makeBubble } from './bubble';
export { makeEventManager, EVENT_TYPE } from './eventManager';

// export default ({ element, props = {} }) => {
//     const manager = makeEventManager();
//     return makeBubble(element, manager, props);
// };
